// João Victor de Jesus Augusto PD015
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

// Importa o CSS global da aplicação
import "./styles.css";

// Cria a raiz da aplicação React e renderiza o App
ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
